/**
 * status translations dict
 */
export default <{ [name: string]: string }>{
  open: 'actions.planned',
  inProgress: 'actions.inProgress',
  finished: 'actions.completed',
  canceled: 'actions.cancelled',
};
